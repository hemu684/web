<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>protfolio</title>
    <style>
        :root{
            --nav-height: 70px; /* height used for the fixed navbar */
        }
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body{
            font-family: Arial, sans-serif;
            /* reserve space at top so content is not hidden under the fixed navbar */
            padding-top: var(--nav-height);
        }
        .logo {
            position: relative;
            left: 45px;
            color: white; 
        }
        nav{
            background-color: #2c3e50; 
            display: flex;
            align-items: center;
            /* fix navbar to top and make its height explicit so we can offset content */
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            height: var(--nav-height);
            padding: 0 30px; /* horizontal padding only; vertical space is controlled by height */
            z-index: 1000;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        
        .right{
            width: 50%;
            margin-left: auto; 
            padding-left: 20px; 
        }
        .right ul{
            display: flex;
            gap: 70px;
            align-items: center;
            justify-content: flex-start;
        }
        .right ul li{
            list-style: none;
            display: flex;
            align-items: center;
            margin: 0 15px;  
            padding: 10px 0;
            
        }
        .right ul li a{
            text-decoration: none;
            color: white;
            font-weight: 500;
            transition: color 0.3s ease; 
        }

        .right ul li a:hover {
            color: #00ffff; 
        }

        @media (max-width: 600px) {
            .right{
                width: 100%;
                margin-left: 0;
                padding: 10px 16px;
            }
            .right ul{
                justify-content: center;
                gap: 16px;
                flex-wrap: wrap;
            }
        }
        /* ---------- Header ---------- */
        
         header {
            text-align: center;
            padding: 120px 20px 60px;  /* Increased top padding to account for fixed nav */
            background: linear-gradient(135deg, #00adb5, #393e46);
            color: white;
            margin-top: -70px;  /* Compensate for body padding */
        }

        header h1 {
            font-size: 2.5em;
            animation: fadeIn 2s ease-in-out;
            margin-bottom: 20px;
            font-weight: 700;
        }

        header p {
            font-size: 1.2em;
            margin-top: 10px;
            opacity: 0.9;
        }
        /* ---------- Sections ---------- */
        
        
        section {
            padding: 50px 20px;
            max-width: 900px;
            margin: auto;
            /* ensure in-page anchor jumps don't end up hidden under the fixed nav */
            scroll-margin-top: calc(var(--nav-height) + 10px);
        }

        h2 {
            text-align: center;
            margin-bottom: 20px;
            color: #00adb5;
        }

        .about p {
            text-align: center;
            line-height: 1.6;
        }


        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            border: 1px solid #aaa;
            padding: 10px;
            text-align: center;
        }

        th {
            background-color: #00adb5;
            color: white;
        }

        tr:nth-child(even) {
            background-color: #e0f7fa;
        }

        tr:hover {
            background-color: #b2ebf2;
        }

         .projects {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 20px;
        }

        .card {
            width: 250px;
            background: white;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            transition: transform 0.3s;
            padding: 15px;
        }

        .card:hover {
            transform: scale(1.05);
        }

        .card h3 {
            color: #393e46;
        }

         form {
            display: flex;
            flex-direction: column;
            gap: 10px;
            width: 80%;
            margin: 20px auto;
        }

        input, textarea, button {
            padding: 10px;
            border: 1px solid #aaa;
            border-radius: 5px;
            font-size: 1em;
        }

        button {
            background-color: #00adb5;
            color: white;
            border: none;
            cursor: pointer;
            transition: background 0.3s;
        }

        button:hover {
            background-color: #008c96;
        }

        /* ---------- Footer ---------- */
        footer {
            text-align: center;
            padding: 15px;
            background: #222;
            color: white;
            margin-top: 40px;
        }

        /* ---------- Animations ---------- */
        @keyframes fadeIn {
            from {opacity: 0; transform: translateY(-20px);}
            to {opacity: 1; transform: translateY(0);}
        }
    </style>
</head>
<body>
    <nav>
            <div class="logo">
                <h3>Himanshu's protfolio</h3>
            </div>
            <div class="right">
                <ul>                   
                    <li><a href="#about">About</a></li>
                    <li><a href="#skills">Skills</a></li>
                    <li><a href="#projects">Projects</a></li>
                    <li><a href="#contact">Contact Me</a></li>
                </ul>
            </div>
        </nav>

    <!-- Header -->
    <header>
        <h1>Welcome to Raj's Portfolio</h1>
        <p>Frontend Developer | Learner | Tech Enthusiast</p>
    </header>

    <!-- About Section -->
    <section id="about" class="about">
        <h2>About Me</h2>
        <p>Hello! I am Raj, a passionate web developer who loves designing responsive and interactive websites using HTML, CSS, and JavaScript.</p>
    </section>

    <!-- Skills Section -->
    <section id="skills">
        <h2>My Skills</h2>
        <table>
            <tr>
                <th>Technology</th>
                <th>Proficiency</th>
            </tr>
            <tr>
                <td>HTML</td>
                <td>⭐⭐⭐⭐⭐</td>
            </tr>
            <tr>
                <td>CSS</td>
                <td>⭐⭐⭐⭐</td>
            </tr>
            <tr>
                <td>JavaScript</td>
                <td>⭐⭐⭐</td>
            </tr>
        </table>
    </section>

    <!-- Projects Section -->
     <!-- Projects Section -->
    <section id="projects">
        <h2>Projects</h2>
        <div class="projects">
            <div class="card">
                <h3>Project 1</h3>
                <p>Responsive Portfolio Website using HTML, CSS, JS.</p>
            </div>
            <div class="card">
                <h3>Project 2</h3>
                <p>Student Feedback Form with Validation.</p>
            </div>
            <div class="card">
                <h3>Project 3</h3>
                <p>Simple To-Do App with JS Interactivity.</p>
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section id="contact">
        <h2>Contact Me</h2>
        <form>
            <input type="text" placeholder="Your Name" required>
            <input type="email" placeholder="Email" required>
            <textarea rows="4" placeholder="Your Message"></textarea>
            <button type="button" onclick="submitForm()">Send Message</button>
        </form>
    </section>

     <footer>
        <p>&copy; 2025 Raj’s Portfolio | Made with 💙</p>
    </footer>
</body>
</html>