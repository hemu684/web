<!DOCTYPE html>
<html lang="en">
<head>
    <!-- <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> -->
    <title>Document</title>
    <style>
       
        /* ---------- Global Styles ---------- */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            background-color: #f5f5f5;
            color: #333;
            scroll-behavior: smooth;
        }

        /* ---------- Navbar ---------- */
        nav {
            background-color: #222;
            color: white;
            padding: 15px 0;
            position: sticky;
            top: 0;
            z-index: 100;
        }

        nav ul {
            list-style: none;
            display: flex;
            justify-content: center;
            gap: 30px;
        }

        nav a {
            text-decoration: none;
            color: white;
            font-weight: 500;
            transition: color 0.3s;
        }

        nav a:hover {
            color: #00adb5;
        }

        /* ---------- Header ---------- */
        header {
            text-align: center;
            padding: 120px 20px 60px;  /* Increased top padding to account for fixed nav */
            background: linear-gradient(135deg, #00adb5, #393e46);
            color: white;
            margin-top: -70px;  /* Compensate for body padding */
        }

        header h1 {
            font-size: 2.5em;
            animation: fadeIn 2s ease-in-out;
            margin-bottom: 20px;
            font-weight: 700;
        }

        header p {
            font-size: 1.2em;
            margin-top: 10px;
            opacity: 0.9;
        }

        /* ---------- About Section ---------- */
        section {
            padding: 50px 20px;
            max-width: 900px;
            margin: auto;
        }

        h2 {
            text-align: center;
            margin-bottom: 20px;
            color: #00adb5;
        }

        .about p {
            text-align: center;
            line-height: 1.6;
        }

        /* ---------- Skills Table ---------- */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            border: 1px solid #aaa;
            padding: 10px;
            text-align: center;
        }

        th {
            background-color: #00adb5;
            color: white;
        }

        tr:nth-child(even) {
            background-color: #e0f7fa;
        }

        tr:hover {
            background-color: #b2ebf2;
        }

        /* ---------- Projects ---------- */
        .project {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 20px;
        }

        .card {
            width: 250px;
            background: white;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            transition: transform 0.3s;
            padding: 15px;
        }

        .card:hover {
            transform: scale(1.05);
        }

        .card h3 {
            color: #393e46;
        }

        /* ---------- Contact ---------- */
        form {
            display: flex;
            flex-direction: column;
            gap: 10px;
            width: 80%;
            margin: 20px auto;
        }

        input, textarea, button {
            padding: 10px;
            border: 1px solid #aaa;
            border-radius: 5px;
            font-size: 1em;
        }

        button {
            background-color: #00adb5;
            color: white;
            border: none;
            cursor: pointer;
            transition: background 0.3s;
        }

        button:hover {
            background-color: #008c96;
        }

        /* ---------- Footer ---------- */
        footer {
            text-align: center;
            padding: 15px;
            background: #222;
            color: white;
            margin-top: 40px;
            
        }

        /* ---------- Animations ---------- */
        @keyframes fadeIn {
            from {opacity: 0; transform: translateY(-20px);}
            to {opacity: 1; transform: translateY(0);}
        }
    </style>
</head>
<body>
    <nav>
        
            <ul>
                <li><a href="#about">About</a></li>
                <li><a href="#skills">Skill</a></li>
                <li><a href="#projects">Projects</a></li>
                <li><a href="#contact">Contact Me</a></li>
            </ul>
       
    </nav>

    <header>
        <h1>Wellcome to Himanshu's Protfolio</h1>
        <p>Frontend Developer | Learner | Tech Enthusiast</p>
    </header>

    <!-- about section -->

    <section id="about" class="about">
        <h2>About Me</h2>
        <p>Hello! I am Raj, a passionate web developer who loves designing responsive and interactive websites using HTML, CSS, and JavaScript.</p>
    </section>

    <!-- skills section -->
     <section id="skills" class="skills">
        <h2>My Skills</h2>
        <table>
            <tr>
                <th>Technology</th>
                <th>Proficiency</th>
            </tr>

            <tr>
                <td>HTML</td>
                <td>*****</td>
            </tr>
            <tr>
                <td>CSS</td>
                <td>*****</td>
            </tr>
            <tr>
                <td>JavaScript</td>
                <td>*****</td>
            </tr>
        </table>
     </section>

    <!-- projects section -->
     <section id="projects" class="projects">
        <h2>My Projects</h2>
        <div class="project">
            <div class="card">
                <h3>Project 1</h3>
                <p>A responsive website built using HTML, CSS, and JavaScript.</p>
            </div>
            <div class="card">
                <h3>Project 2</h3>
                <p>A responsive website built using HTML, CSS, and JavaScript.</p>
            </div>
            <div class="card">
                <h3>Project 3</h3>
                <p>A responsive website built using HTML, CSS, and JavaScript.</p>
            </div>
        </div>
     </section>

    <!-- contact section -->
     <section id="contact">
        <h2>Contact Me</h2>
        <form action="">
            <input type="text" placeholder="Your Name" required>
            <input type="email" placeholder="Your Email" required>
            <textarea row = "4" placeholder="Your Message" required></textarea>
            <button type="submit">Send Message</button>
        </form>
     </section>

     <footer>
        <p>&copy; 2024 Himanshu. All rights reserved.</p>
     </footer>
</body>
</html>